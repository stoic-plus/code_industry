<h1>Welcome to ScienceFollow</h1>

<%= button_to "Login", login_path, method: :get %>
<%= button_to "Sign Up", new_user_path, method: :get %>

<p>Click a topic below to follow it, (You must be logged in)</p>

<% @topics.each do |topic| %>
  <%= button_to topic.name, topic_path(topic.id), method: :get %>
<% end %>
